import TicketScreen from '@/components/Tickets';
import { SafeAreaView } from 'react-native-safe-area-context'

export default function Tickets() {
  return (
    <SafeAreaView>
     <TicketScreen />
    </SafeAreaView>
  );
}

